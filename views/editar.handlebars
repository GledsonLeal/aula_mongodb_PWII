<h1>Editar Aluno: </h1>
<div class="card">
    <form action="/alunos/editar" method="POST">
    <input type="hidden" name="id" value="{{alunos._id}}">
        <label for="nome">Nome: </label>
            <input type="text" id="nome" name="nome" placeholder="Nome completo do aluno" value="{{alunos.nome}}"><br>
        <label for="email">E-mail: </label>
            <input type="email" id="email" name="email" placeholder="E-mail do aluno" value="{{alunos.email}}"><br>
    <button type="submit">Editar aluno</button>

    </form>
</div>